═══════════════════════════════════════════════════════════════
  ESTUDIO IA · CONEXIÓN REPLICATE  (imagen + video)
  Paquete ÚNICO y FINAL · borra los anteriores que te di
═══════════════════════════════════════════════════════════════

QUÉ ARREGLA ESTE PAQUETE
  ✔ M1 · Imagen  → ahora genera con Replicate (tu crédito)
  ✔ M2 · Video   → ahora genera con Replicate (tu crédito)
  · M3 · Voz     → NO se toca (ya funciona con voz gratis del navegador)
  · M4 · Carpeta → NO se toca (no usa internet)

NO BORRA NADA de tu sistema. No toca tu hub, tu juego ni Firebase.
Tus reglas de Firebase viven en Firebase, no en estos archivos:
subir esto a Netlify NUNCA las afecta.

───────────────────────────────────────────────────────────────
QUÉ CONTIENE  (4 cosas)
  1) netlify/functions/replicate.js  ← el "puente" (NUEVO)
  2) netlify.toml                    ← config del puente (NUEVO)
  3) modulo1_imagen.html             ← reemplaza el tuyo
  4) modulo2_video.html              ← reemplaza el tuyo

───────────────────────────────────────────────────────────────
CÓMO INSTALARLO  (en TU carpeta, la que ya publicas)
  1. Abre tu carpeta del sistema (la de siempre).
  2. Copia dentro la carpeta  netlify  y el archivo  netlify.toml
  3. Reemplaza  modulo1_imagen.html  y  modulo2_video.html
     por los de este paquete.
  4. Arrastra TU carpeta completa a Netlify (pestaña Deploys).
  Listo. Todo lo demás queda igual.

───────────────────────────────────────────────────────────────
CÓMO USARLO  (en el sitio publicado)
  IMAGEN (M1):  elige "Replicate" → pega tu token r8_... → Enter → Generar
  VIDEO  (M2):  elige "Replicate" → pega tu token r8_... → Enter → Generar
  · Si subes una imagen de referencia, el video la usa automáticamente.
  · El video tarda 1-3 min (verás "Replicate procesando %"). Es normal.

  ⚠ Usa tu token ACTIVO de Replicate ("FATIMA CALDEA", r8_aIq...).
     El token "Default" está DESACTIVADO y no sirve.

  ⚠ Solo funciona en el sitio publicado (https), no abriendo el
     archivo en el PC: el puente necesita el servidor de Netlify.
═══════════════════════════════════════════════════════════════
